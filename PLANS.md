# Production system execution plan

This is a living execution plan. It must remain usable by an engineer who has only this repository and the mission specification. Update progress, decisions, discoveries, and verification evidence whenever work advances.

## Purpose and observable outcome

An operator starting from a clean Docker host can configure providers and research subjects in the UI, discover and evidence a subject, approve a dossier and cited script, produce and review media, upload the exact approved artifact privately, and authorize publication separately. Every stage is durable, versioned, auditable, rerunnable, restart-safe, and recoverable.

The authoritative product requirements are the user-supplied mission brief. `docs/implementation-status.md` is a traceability ledger, not permission to narrow that brief.

## Progress

- [x] 2026-07-22: Rework the operator UI around task orientation and progressive disclosure so the main TubeFactory screens are easier to operate without memorizing the backend workflow order.
- [x] 2026-07-22: Prepare production media for real ComfyUI and Voicebox providers: add a validated, accessible, live-preview Brand Kit editor to every versioned Channel; make narration duration authoritative for the production timeline; and composite the generated immutable Scene Media Assets into Remotion instead of displaying their internal visual briefs.
- [x] 2026-07-22: Add a `Use existing research` script-import path: select an active opportunity with an approved explanation-ready source brief, paste author text, adapt it through the LAN model to exact approved claims/evidence, independently verify it, persist an immutable initial or replacement version, and keep approved downstream work protected.
- [x] 2026-07-22: Replace the active FaktischSimpel production recipe with a new 5–10 minute simple-explainer workflow: current science/technology discovery, human topic selection, an automatically accepted trustworthy source brief, reviewed script/storyboard, and automatic coherent media production after storyboard approval; archive every old workflow lineage before clean tests and leave the validated run at topic review.
- [x] 2026-07-22: Improve the blocked Lithium output by generating to a safe length and claim-coverage margin, deterministically rejecting repeated/meta/counterevidence filler, heartbeating long render/QA work, and validating a fresh automatic optimization run.
- [x] 2026-07-22: Make every successful production approval durably and idempotently start its permitted next workflow step, while preserving all later human gates and surfacing configuration blockers instead of requiring a second manual start action.
- [x] 2026-07-20: Confirmed the repository was empty and derived the gated delivery sequence.
- [x] 2026-07-20: Increment 0 foundation runtime acceptance gate passed; browser visual audit remains separately tracked.
- [x] 2026-07-20: Increment 1 discovery and evidence acceptance gate passed.
- [x] 2026-07-20: Increment 2 editorial production acceptance gate passed.
- [x] 2026-07-21: Increment 3 media production acceptance gate passed.
- [x] 2026-07-21: Increment 4 publishing mock/dry-run acceptance gate passed; real network use remains credential-gated.
- [x] 2026-07-21: Increment 5 optimization and hardening acceptance gate passed.
- [x] 2026-07-21: Final reproducible deployment, migration, regression, security, observability and recovery audit passed; real Google action remains operator-credential-gated.
- [x] 2026-07-21: Complete channel/subject lifecycle management passed: prefilled version-checked edits, audited soft deletion, schedule cleanup, dependency protection, responsive operator controls, and live validation.
- [x] 2026-07-21: Add a searchable Archived profiles restore workspace and replace placeholder live-opportunity scoring with evidence-derived, auditable score version 2 traces.
- [x] 2026-07-21: Configure and validate a channel-scoped FaktischSimpel explainer workflow with evidence-safe AI prompts, scheduled topic discovery, operator guidance, live browser evidence, and an illustrated DOCX operations guide.
- [x] 2026-07-21: Correct live discovery so upstream engine failures cannot masquerade as a successful empty run; throttle searches, separate recent from evergreen discovery, expose source health, rerun FaktischSimpel, and correct its illustrated guide.
- [x] 2026-07-21: Archive the 38 legacy FaktischSimpel scored findings without deleting immutable score history, rerun live discovery, take one newly discovered video topic through the script and storyboard human review gates, and publish an updated illustrated DOCX runbook.
- [x] 2026-07-21: Interrupt the stale FaktischSimpel generation run; discover provider models safely through the worker; route all five editorial/assessment tasks to `gemma4-e4b:64k`; add AI-assisted topic qualification and source-bound evidence review behind deterministic gates with transparent hybrid scoring, confidence and abstention; archive the prior test lineage; and begin a fresh LAN-AI optimization series.
- [x] 2026-07-22: Add bounded verifier-guided script self-correction, prove that unsupported interpretations are regenerated without manual text edits, then approve a satisfactory FaktischSimpel script and complete its source-linked storyboard.
- [x] 2026-07-22: Add generic Explanation Readiness before script generation, bounded AI-guided evidence enrichment, autonomous handoff between approved candidates, duration/claim/coverage gates for scripts and concrete-visual gates for storyboards; deploy and validate the complete LAN-AI series.

## Milestones

### Increment 0 — Foundation

Deliver the monorepo, pinned Compose topology, local auth/RBAC, optional TOTP-ready secret handling, PostgreSQL/Alembic, MinIO, Temporal, base API/UI, immutable audit events, versioned configuration, health checks, CI, and a durable probe workflow. The gate passes only when `docker compose up --build -d` becomes healthy and a running probe remains queryable and completable after the worker is recreated.

### Increment 1 — Discovery and evidence

Deliver profiles, scheduled search plans, safe acquisition, immutable snapshots, clustering/scoring, research plans, atomic claims/evidence, and dossier review. Validate against deterministic fixtures including duplicate and hostile-source cases.

### Increment 2 — Editorial production

Deliver the policy-enforcing AI gateway, registered model routing, prompts, cited script versions, verification, the side-by-side editor, SceneSpec schema, and storyboard versions. Prove unsupported factual narration cannot progress.

### Increment 3 — Media production

Deliver provider-neutral ComfyUI and Voicebox ports/adapters, registries, narration/alignment, Remotion rendering, FFmpeg validation, manifests, asset licences, and blocking QA.

### Increment 4 — Publishing

Deliver encrypted OAuth tokens, dry-run by default, resumable private upload, processing reconciliation, exact-version approval binding, captions/thumbnail, and a separate idempotent scheduling/publication transition.

### Increment 5 — Optimization and hardening

Deliver analytics, benchmark recommendations that cannot change routing before human approval, freshness/corrections/originality policies, budgets, observability, backup/restore drill, SBOM, and security hardening.

## Architectural constraints

Domain entities and policies in `packages/core` import no web framework, ORM, Temporal SDK, provider SDK, or storage SDK. Application ports are defined inward and implemented by adapters. FastAPI controllers and Temporal workflows translate transport data into plain application inputs. External calls occur only in Temporal activities and use persisted idempotency keys.

PostgreSQL is authoritative for metadata and workflow-facing state; immutable large bodies and media live behind an S3 abstraction in MinIO. Source snapshots and audit events are append-only at both the application and database levels. Temporal uses separate `temporal` and `temporal_visibility` databases.

## Decisions

- 2026-07-22: Treat the usability issue as workflow orientation first and visual polish second. Preserve the existing API/backend behavior, but make the shell expose the production path, keep primary queues visible, and move secondary creation/import/provider registry controls behind explicit disclosures.
- 2026-07-22: Keep Channel branding as a validated versioned Brand Kit within the existing Channel profile rather than adding a mutable global theme. The renderer receives a normalized snapshot plus Channel version and Brand Kit hash for each production. Operators edit semantic colors, typography families, mark, tagline, corners, motion and visual direction through accessible controls with a 16:9 preview; arbitrary CSS, remote font URLs and raw brand JSON are not accepted.
- 2026-07-22: Generate and master narration before Scene Media Assets, then derive production scene durations from the actual narration segments without mutating the approved Storyboard SceneSpecs. Pass only time-limited internal MinIO asset URLs to Remotion and retain only immutable asset hashes and object keys in the Production Manifest, never signed URLs.
- 2026-07-22: Treat pasted scripts as untrusted author input, not as a way around research. The import selects an active opportunity, resolves its exact current approved explanation-ready dossier, preserves the raw input in the immutable workflow control record, and lets the LAN writer adapt only source-supported material before deterministic and independent verification. If an unapproved script already exists, import creates a new immutable version; approved scripts, locked segments and existing storyboards block replacement.
- 2026-07-22: For the FaktischSimpel simple-explainer recipe, retain immutable source snapshots and source-bound factual claims as internal quality controls, but replace the separate human dossier gate with an audited automatic source-brief acceptance after deterministic readiness succeeds. Topic, script, storyboard, exact render and publication remain human decisions; storyboard approval starts media production automatically.
- 2026-07-22: Treat archival of old test data as a systemwide root-lineage operation. Archiving every active opportunity keeps immutable scores, evidence and audit records, while active dossier, script, storyboard, media and publishing lists follow only non-archived opportunity lineages.
- 2026-07-22: Separate topic discovery from evidence research for the simple-explainer workflow. The topic radar searches curated current science sources and rejects navigation/institutional announcement noise; primary, falsification and broader evidence searches begin only after the human chooses a topic.
- 2026-07-22: Treat approval as the command that authorizes entry into the next non-human stage. Start that continuation server-side with a stable target-version idempotency key; never remove or pre-authorize the following human gate, and report missing downstream configuration as an explicit awaiting-input result.
- 2026-07-20: Preserve the brief's increment order and refuse to advance past a failing mandatory gate.
- 2026-07-20: Use a modular monorepo with clean dependency direction; deployment services remain independently scalable without sharing framework models.
- 2026-07-20: Use explicit development secrets only to make a clean local clone bootable. Production operation requires replacement secret files and TLS.
- 2026-07-20: Treat optional-profile capability as unavailable until its real behavior and tests exist; never return fake success.
- 2026-07-21: Keep Google credentials and plaintext refresh tokens inside a dedicated publisher egress gateway; return only ciphertext to the API and never place secrets in Temporal history.
- 2026-07-21: Give dry-run and real uploads distinct stable release keys so a safe rehearsal never consumes or aliases the real external side effect.
- 2026-07-21: Expose user-requested profile deletion as recoverable archival. Preserve linked workflows, evidence and audit history; archive subjects only after stopping their schedule, and block channel archival while active subjects remain.
- 2026-07-21: Restore archived profiles as disabled records. Require the parent channel to be active before restoring a subject, keep its schedule paused, and create a new audited version instead of erasing archive history.
- 2026-07-21: Treat channel automation instructions as trusted style refinements only. They may shape explanation structure, language and visuals but cannot relax approved-claim, exact-evidence, independent-verification, JSON-schema, human-approval or private-publication gates.
- 2026-07-21: Add AI qualification as advisory, source-bound structured assessments rather than a truth oracle. Deterministic source, independence, citation, approval and publication gates remain authoritative; AI confidence, disagreement and abstention must be persisted and visible.
- 2026-07-21: Compute any hybrid opportunity score deterministically from separately visible deterministic and AI-derived dimensions. Never accept an opaque model-supplied total, and retain every prior score and review version append-only.
- 2026-07-21: Archive the completed FaktischSimpel test lineage recoverably while retaining immutable sources, evidence, scores, model usage and audit history; the next optimization series receives new workflow and idempotency identifiers.
- 2026-07-22: Feed independent-verifier findings back into the writer through at most three bounded correction passes. Only segments named by the verifier and permitted by the active regeneration selection may change; deterministic evidence validation and all independent verifier passes rerun after every correction, and unresolved output remains blocked.
- 2026-07-22: Give every approved claim exactly one owner beat, consolidate near-duplicate claim IDs into that beat, and generate toward a 15% word-count margin above the hard minimum. Per-beat claim and length contracts reject incomplete model chunks before assembly.
- 2026-07-22: Combine whole-script review and editorial-language review with a focused factual-entailment audit that receives only each sentence's explicitly linked normalized claims and exact excerpts. Selected regeneration scopes AI findings to changed segments, while deterministic whole-script checks remain global.
- 2026-07-22: Heartbeat every 30 seconds while awaiting Remotion, narration retrieval, muxing, probing, FFmpeg QA and thumbnail extraction so a lost media worker is retried promptly without shortening legitimate renders.
- 2026-07-22: Treat verifier severity as authoritative consistency data: any `severity=error` forces `valid=false` even when a local model emits the contradictory boolean `valid=true`. Pure rhetorical questions, transitions, navigation, and source-review invitations remain valid editorial language when they contain no checkable assertion.
- 2026-07-22: Use the B1 protocol surfaces by client contract: OpenAI-compatible clients use `http://ai.b1.germering:11438/v1`, native Ollama clients use `http://ai.b1.germering:11434`, and `gemma4-e4b:64k` is assigned on both the primary and native fallback routes.
- 2026-07-22: Enforce storyboard visual diversity deterministically after generation and during alternatives. Visual roles and briefs remain source-bound; compact semantic model output is assembled locally into the immutable SceneSpec contract.
- 2026-07-22: Derive explanation readiness from each subject's duration, speaking rate, evidence density, explanation-unit and enrichment policies. AI may plan searches and propose source-bound claims, but deterministic gates decide readiness and prevent legacy or under-evidenced dossiers from reaching script generation.
- 2026-07-22: After exhausting a bounded evidence loop, automatically hand off only to another already human-approved candidate in the same subject. Acquisition and a new research lineage are created by child workflows; missing approval or exhausted candidates end in an explicit insufficient-evidence state rather than weakening the gate.
- 2026-07-22: Bound Temporal evidence payloads to the 24 highest-ranked independent sources and strip semantic chunks from AI/readiness calls. Immutable snapshots remain retained, while activity inputs stay below Temporal's per-command size limit.

## Discoveries

- 2026-07-22: Live browser inspection found the highest-friction UI issues on profiles, research, editorial and providers. Profile and research screens show create forms before or beside the active review queues, editorial opens with a large paste-script import form even when no eligible source brief exists, opportunity status chips stretch into tall vertical pills, the mobile navigation wraps labels into fragments, and provider model discovery renders as a long unsearchable list.
- 2026-07-22: The media worker already stores one immutable visual asset per SceneSpec, but the Remotion request omits those assets and renders the internal `visual_brief` as viewer-facing text. The active `fixture-scene` and `fixture-narrator` providers therefore test orchestration only. The provider-ready correction is to render the same stored asset contract for fixture and real ComfyUI outputs, with narration-derived timing and a hashed Channel Brand Kit snapshot.
- 2026-07-22: The clean simple-explainer test lineage currently has nine active topic candidates but no source brief yet, so the deployed import selector correctly stays empty until an operator selects a topic and its automatic research completes. A live import attempt against an active candidate without a brief returned the intended `409` and created no workflow.
- 2026-07-22: Topic qualification heartbeated only while one model call exceeded 20 seconds. A batch of many individually faster calls could therefore exceed Temporal's one-minute heartbeat timeout without emitting any heartbeat. Candidate-boundary heartbeats now cover the whole batch as well as long individual calls.
- 2026-07-22: The first simple-explainer test bypassed discovery hygiene because the filter matched only the legacy literal `evidence_first_explainer`; `simple_explainer` therefore admitted search homepages, support forums and unrelated results. All explainer targets now share the filter, and configurable AI confidence/dimension minimums can archive weak candidates before human review.
- 2026-07-22: Running primary-evidence strategies during topic discovery made an ArXiv rate limit fail the entire radar before any choice existed. The new workflow defers evidence strategies until after selection, matching its editorial contract and keeping evidence availability from masquerading as a lack of topics.
- 2026-07-20: The repository contained only an empty Git repository on branch `dev`.
- 2026-07-20: The host has Docker 29.1.3 and Compose 2.40.3; host Python is 3.14 and npm is absent, so validation must run in pinned containers.
- 2026-07-20: The installed ExecPlan skill omitted its required `references/PLANS.md`; this plan therefore embeds all context needed for continuation.
- 2026-07-21: Live score concentration at 53 is caused by placeholder constants in `persist-live-opportunities`: five positive dimensions and two penalties are constant, while the remaining formulas receive the same one-result/one-domain inputs for most clusters. PostgreSQL confirmed 175 active opportunities at 53 and only seven at other values.
- 2026-07-21: FaktischSimpel existed as enabled channel version 2 with its German description and evidence-first rule, but had no subject profile and the editorial workers selected only one global prompt per task. Channel-specific tone therefore requires bounded additional system instructions resolved through the dossier-to-channel lineage, while retaining the global strict response contracts.
- 2026-07-21: Initial script generation accidentally referenced regeneration-only selection fields, while the regeneration workflow did not receive them. The operational instruction was moved to regeneration and both paths now receive the channel workflow through their lineage.
- 2026-07-21: A clean editorial-worker build exposed stale pre-TubeFactory dependency names in the editorial and publisher package metadata. The package requirements now match the renamed local distributions, so the result no longer depends on an old Docker layer.
- 2026-07-21: The empty FaktischSimpel run was not a valid lack-of-topics outcome. SearXNG returned HTTP 200 with zero results while reporting Brave rate limiting, DuckDuckGo CAPTCHA, Startpage CAPTCHA suspension and Wikidata access denial; the client discarded `unresponsive_engines`, seven searches were launched concurrently, and the UI therefore rendered orchestration completion as successful source coverage.
- 2026-07-21: The live LAN models can require more than ten minutes for a long schema-constrained FaktischSimpel script. The former AI gateway also imposed a hidden 90-second socket-read timeout. HTTP timeouts now honor the explicit model deadline, and long local generation uses a bounded 30-minute model deadline inside a 35-minute Temporal activity rather than enabling a remote fallback.
- 2026-07-21: B1 exposes two intentionally different protocol surfaces: OpenAI-compatible clients use `http://ai.b1.germering:11438/v1`, while native Ollama clients use `http://ai.b1.germering:11434`. Both were live-verified from the editorial worker; the active TubeFactory routes use the OpenAI-compatible surface and its inventory contained 30 models including `gemma4-e4b:64k`.
- 2026-07-21: Archived opportunities were still hard-blocking their source URLs in later discovery runs. URL suppression now considers only active opportunities in the same subject; archived history remains available to deterministic similarity and duplication scoring without preventing a new optimization series.
- 2026-07-21: The first hybrid evidence assessment correctly abstained at 0.50 confidence because six acquired records were aliases or versions of the same arXiv paper and the deterministic gate found zero independent supporting sources. This is a useful optimization result, not a reason to override the evidence gate.
- 2026-07-22: The dual script verifier rejected 100%-cited drafts because the writer repeatedly converted supported demand and scenario claims into unsupported claims about strategic importance, supply security, operating state, and feasibility. Prompt-only retries were insufficient, so verifier findings must become structured input to an automatic bounded rewrite loop rather than operator-authored prose edits.
- 2026-07-22: Repeating invalid JSON lengths of 1,448, 2,896, and 4,344 bytes were not model hallucinations. Both model calls and native model discovery used one `StreamReader.read(maximum)` call, which may return the currently buffered chunk before EOF. Bounded chunk iteration now reads through EOF while rejecting the first byte over the configured ceiling.
- 2026-07-22: Local AI routing is protocol-specific: OpenAI-compatible clients use `http://ai.b1.germering:11438/v1`; native Ollama clients use `http://ai.b1.germering:11434`. All seven active editorial/research task assignments use `gemma4-e4b:64k` on the OpenAI-compatible route with the same model on the native Ollama route as fallback.
- 2026-07-22: A model verifier can miss material repetition or mistake factual-sounding source-gap prose for harmless editorial language. Script refinement now combines two AI reviews with a deterministic repeated-fact-block gate, segment-specific extractive fallbacks, a source-safe counterevidence scaffold when no reviewed counterclaim exists, and a topic-specific but non-assertive hook.
- 2026-07-22: The former 410-word/182-second FaktischSimpel result was formally cited but could not substantiate a 6–10-minute explainer with only three factual claims. The new profile requires 810–1,350 words, at least 13 distinct factual claims and six covered explanation units before any script can be generated.
- 2026-07-22: Passing 48 extracted sources plus semantic chunks through Temporal exceeded `ScheduleActivityTaskCommandAttributes.Input`. Compact AI inputs and a 24-source deterministic evidence set crossed the same live stage successfully; legacy snapshot metadata also required accepting `reputation` as either an object or serialized JSON string.
- 2026-07-22: The autonomous live series improved the automatically selected health candidate to six usable claims, eight independent sources, five primary sources, complete six-unit coverage and completed counterevidence search. It correctly remained blocked because six claims are still below the 13-claim floor, and no further human-approved candidate existed.
- 2026-07-22: Rich topics were under-producing claims because each enrichment synthesis replaced the previous result, the explanation plan could be redesigned between rounds, and the model marked almost every proposal central. Lithium had 27 proposals across four rounds but only the final seven reached persistence; the health candidate had 25 proposals and 20 textually distinct statements across its rounds but only six survived in the final synthesis.
- 2026-07-22: Prompt instructions alone did not stop Gemma 4 from repeating six of seven prior claims in a later enrichment round. The generic correction therefore maintains a deterministic cumulative claim bank, merges near-paraphrases with their exact evidence links, preserves the initial coverage-unit IDs, targets missing or thin units, and withholds previously used Evidence IDs from subsequent synthesis inputs.
- 2026-07-22: Evidence independence is a property of a source/work group, not of how many excerpts from that group happen to be cited. Links now retain their dependency key and readiness counts the distinct key set; aliases still count once, while a weakly supported detail can remain non-central instead of causing wholesale claim loss. Social/catalog sources cannot become primary evidence merely from a mislabeled source type.
- 2026-07-22: Redeploying an editorial worker during a long local-model activity exposed the absence of activity heartbeats: Temporal retained the dead worker's started activity until start-to-close timeout. Advisory AI activities now heartbeat while awaiting the LAN model and research workflows use a one-minute heartbeat timeout, preserving long healthy calls while making worker loss retry promptly.
- 2026-07-22: A later enriched dossier reached `SYNTHESIZING_DOSSIER` but Temporal rejected the persist command because accumulated semantic-chunk embeddings exceeded the command input limit. Acquisition already stores these chunks transactionally, so dossier persistence now receives only the bounded source metadata and evidence excerpts; LAN-model context is independently capped at 16 works, four excerpts per work and 2,400 characters per excerpt.
- 2026-07-22: The corrected Lithium revision `live-research-dossier-corrected-claim-bank-feedback-v2-lithium-20260722a` completed autonomously in `DOSSIER_REVIEW`: 14/13 usable claims, all seven explanation units covered, 9 independent and 6 primary sources, counterevidence search complete, and a non-abstaining 14-claim/9-source AI review at confidence 1.0. The final persist command carried 24 compact sources without exceeding Temporal limits. Later synthesis rounds now stable-sort previously unused work identities ahead of already cited works to reduce paraphrase churn without removing the fallback evidence.
- 2026-07-22: Browser reproduction showed that dossier approval returned HTTP 200 and persisted `approved`, but the detail view retained active Approve/Reject controls, making success appear ineffective and permitting immediate duplicate actions. The web client now exposes an in-flight label/lock, reloads the versioned result, and replaces all dossier/claim review controls with a persistent recorded-decision status for terminal dossier versions.
- 2026-07-22: Follow-up browser UI inspection found three persistent usability problems after the first shell rework: sidebar hover help obscured neighboring navigation items, production path details were ellipsized into unusable fragments, and the Research board/Providers ledger still wasted space in common empty or long-history states. Navigation guidance now lives in the explicit Help panel/native titles, production path details wrap, empty dossier queues no longer squeeze the Opportunity board, and the AI usage ledger is searchable/capped by default.
- 2026-07-22: The approved Lithium dossier still held all 14 accepted claims in `supported`, while the script gate correctly required `approved`. Exact dossier approval now batch-approves only supported claims, preserves explicit claim rejections, audits the affected claim IDs, and starts script generation only after the resulting readiness check passes.
- 2026-07-22: Script-global errors were previously masked whenever a verifier also returned segment warnings, so only warned segments were rewritten. Global blockers now select every permitted unlocked segment, while warnings cannot narrow the correction scope.
- 2026-07-22: Gemma sometimes repeated a UUID inside one sentence's claim list. The model-facing chunk schema now permits that harmless transport duplication and the assembler canonicalizes IDs; the persisted evidence boundary remains strict and duplicate-free.
- 2026-07-22: Persisting regeneration used to discard the verifier's decision and leave an unnecessary separate verification action. Regeneration now stores the independent report, model and prompt IDs and transitions directly to `verified` or `blocked`.
- 2026-07-22: Re-verifying an unchanged whole script after selected regeneration allowed local-model drift to invent new findings on untouched segments. AI issue acceptance is now scoped to the regenerated selection, while deterministic safety and format checks still evaluate the complete merged draft.
- 2026-07-22: Three near-identical geothermal-brine claims forced repeated narration despite unique claim ownership by ID. Semantic near-duplicate grouping now assigns the IDs to one owner sentence instead of making the writer restate the same concept in adjacent beats.
- 2026-07-22: Correction targets inherited and then increased the length of already flawed prose, rewarding filler. When the whole script is already above its format target, correction now sizes a beat by distinct evidence concepts and deterministically retains only sentences that introduce new approved claim coverage; declarative counterevidence padding is discarded before verification.
- 2026-07-22: A JSON-schema `maxItems` experiment was too brittle for the active local model and exhausted three activity attempts. The failed workflow created no script version. Sentence bounding therefore lives in deterministic post-generation compaction, while the audited provider schema remains compatible with the model.

## Validation and acceptance evidence

2026-07-22 Channel Brand Kit and provider-ready media evidence:

- The structured Brand Kit is live at **Channels & subjects → Channel profiles → Brand Kit** and remains available through **Edit**. A real browser login showed Brand Kit previews on both Channel cards, opened the FaktischSimpel editor, exposed labelled identity/color/type/motion controls, and displayed the 16:9 preview. Screenshots were saved at `output/playwright/brand-kit-channel-cards.png` and `output/playwright/brand-kit-editor.png`.
- Every API response now expands old or partial Brand Kits into the 15-field schema. Semantic colors reject CSS and unsafe URLs and enforce WCAG AA contrast; CamelCase Channel defaults derive distinct initials (`FaktischSimpel` → `FS`, `FakeBuster` → `FB`).
- Media production generates narration first, retains the approved SceneSpec duration separately, and derives each runtime scene/chapter duration from its actual mastered narration segments. Missing or duplicate Scene Media Assets fail closed.
- Remotion receives the exact immutable image/video asset for each SceneSpec plus the normalized Brand Kit snapshot. A live one-second smoke render fetched an existing PNG from internal MinIO through a ten-minute signed URL and returned a valid 99,530-byte `video/mp4`; the signed URL is excluded from manifests and provenance.
- API/Core tests pass 59 plus four focused post-build branding tests; editorial-worker tests pass 69; renderer tests pass four; Next.js TypeScript and production builds pass; Compose validation and `git diff --check` pass. API, editorial worker, renderer and web are healthy after deployment.

2026-07-22 operator UI usability rework evidence:

- A real browser audit after deployment captured the confusing states on `output/playwright/ux-click-dashboard.png`, `output/playwright/ux-click-research.png`, `output/playwright/ux-click-providers.png` and `output/playwright/ux-click-mobile-research.png`: obstructive sidebar hover cards, ellipsized production-path text, a squeezed Opportunity board beside an empty dossier column, an uncapped AI usage ledger and a floating mobile Help button over content.
- The deployed UI now shows readable production-path labels, no obstructing sidebar hover cards, a full-width Opportunity board when no dossier queue exists, a searchable/capped Safe Usage Ledger, and a mobile Help button in the top chrome instead of over the work area. Final screenshots were saved at `output/playwright/ux-final-dashboard.png`, `output/playwright/ux-final-research.png`, `output/playwright/ux-final-providers.png` and `output/playwright/ux-final-mobile-research.png`.
- A follow-up live audit of Media and Publishing found disabled downstream controls without an explicit prerequisite path. The deployed pages now show readiness cards, missing prerequisite counts, next-stage navigation buttons and disabled button labels that state the required action. Final screenshots were saved at `output/playwright/ux-media-readiness-final.png` and `output/playwright/ux-publishing-readiness-final.png`.
- A follow-up Operations audit found an unstyled `stat-grid` summary and too many low-frequency admin forms exposed at once. The deployed Operations page now has stable summary cards, a task chooser, visible daily analytics/recoverability sections, and explicit Open/Close disclosure headers for evidence registry, model benchmark, governance gates and corrections. Final screenshots were saved at `output/playwright/ux-operations-disclosure-final.png`, `output/playwright/ux-operations-mobile-disclosure-final.png` and `output/playwright/ux-operations-benchmark-open.png`.
- A final lower-traffic-page audit covered Workflow Activity, Audit Log, Archive, Configuration and Users. Workflow Activity now groups repeated failed workflows by type with failure-triage cards, stage-specific next-step hints and one-click filtering for repeated groups.
- Audit Log now supports event search and action filtering by action, target or correlation ID. Users now supports account search and role filtering with a scroll-contained account list and correct singular/plural result copy.
- Configuration now shows live JSON validity, current active version/hash context, draft-change state and disables activation when JSON is invalid. Archive now renders in progressive batches so large retained history remains searchable without overloading the page.
- `npm --prefix apps/web test`, `npm --prefix apps/web run build`, containerized web-image `npm test` and `next build`, `git diff --check`, Compose health checks and `/api/health/ready` all passed after the deployed web container was recreated. Playwright captured live desktop evidence for workflow triage, audit filtering, user filtering, configuration safety, archive progressive loading, and verified interactions for audit search, user role search, workflow group filtering and invalid-JSON blocking.

2026-07-22 `Use existing research` evidence:

- The API suite passes 115 tests and the editorial-worker suite passes 67 tests. The production web image passes TypeScript validation and the optimized Next.js build; `git diff --check` is clean.
- The deployed API, editorial worker, web and Caddy containers are healthy. The live route accepts only editor-authorized requests for an active Opportunity with an approved, explanation-ready source brief and approved claims; approved scripts, locked segments and existing storyboards block replacement.
- A live guarded request against an active Opportunity without a source brief returned `409 Use existing research requires an approved source brief for this opportunity`; no workflow or editorial artifact was started.
- A real browser login at `http://evidence-studio.local:8090` showed the `Use existing research` panel in Scripts and storyboards with the Opportunity selector, title, bounded paste field, evidence-bound import action and source-brief-specific AI prompt. With no completed source brief in the current clean lineage, the selector is empty and the action is disabled as designed.
- Imported text is marked as untrusted author input, retained with its SHA-256 and character count in the durable control/audit trail, adapted by the configured LAN writer only to approved claims, and passed through deterministic plus independent verification before an immutable initial or replacement script version is persisted.

2026-07-22 FaktischSimpel simple-explainer evidence:

- API, research-worker and editorial-worker suites pass with 114, 71 and 64 tests; the production web build and TypeScript validation pass, Python modules compile, JSON configuration validates and `git diff --check` is clean.
- The audited systemwide archive action moved all 102 remaining active fixture/schedule findings into the archive after 59 FaktischSimpel and 178 FakeBuster records had already been archived. PostgreSQL then reported `0` active and `364` archived opportunities, with `0` active dossier, script and storyboard lineages before the replacement test began.
- Final workflow `live-discovery-faktischsimpel-simple-explainer-v4-20260722` completed at the intended human topic gate. It reduced 21 raw metasearch results to nine concrete science/technology candidates, automatically rejected 12 navigation or institutional-announcement results, and retained sources from four curated domains.
- All nine topic qualifications were produced by `gemma4-e4b:64k` through the local OpenAI-compatible provider `http://ai.b1.germering:11438/v1`; no test candidate was inserted or edited manually. The remaining topic choices include battery ageing, defossilized chemistry, flood resilience, digital companions in ageing, biomedical cell research, mass extinction, dark matter and interstellar chemistry.
- The live subject is version 6 with a 300–600 second format, automatic source brief, human topic/script/storyboard/media/publication gates, evidence research after topic selection, and a configurable topic-review minimum policy. Its final run is complete and waiting for the operator's topic choice.

2026-07-22 Lithium output optimization evidence:

- The editorial-worker suite passes 63 tests. It covers safe target margins, unique claim ownership, near-duplicate consolidation, global correction scope, selected verifier scope, focused factual entailment, direct regeneration persistence, repeated/meta/counterevidence gates, concept-sized correction and deterministic sentence compaction.
- The original blocked Lithium script had 768 words, an estimated 341.3 seconds, only 11 distinct claim IDs and repeated uncertainty/factual material. Live version 3 reached 1,131 words, 502.6 seconds, all 14 approved claim IDs, 100% central coverage and zero blockers through the normal LAN-AI workflow.
- The final live optimization workflow `script-regeneration-lithium-quality-v13-compacted-20260722a` persisted current script version 12 as `verified`: 457.2 seconds, all 14 approved claim IDs, 100% coverage and no blocking issues. The two problematic counterevidence beats were reduced from 165 words in the earlier draft to 55 evidence-bearing words without database edits or a manual verification step.
- A stricter optional polish workflow, `script-regeneration-lithium-quality-v14-specific-limits-20260722a`, exhausted its structured-output activity attempts and failed without persisting a version; verified version 12 remained current. This proves failure does not replace the last accepted immutable version.
- Media workflow `media-production-heartbeat-render-v2-20260722a` completed on its first attempt as `MEDIA_READY` while emitting `waiting_for_remotion_render` heartbeats during the long render. Its enhanced QA verdict was `warn`, not `fail`: stream, duration and artifact checks passed, while frozen-frame and caption-readability warnings remained review-visible for the older storyboard.

2026-07-22 approval-continuation evidence:

- API, research-worker and editorial-worker suites passed with 112, 66 and 49 tests respectively; the production Next.js build and TypeScript validation passed, and `git diff --check` is clean.
- The deployed API, research worker and web containers are healthy, and `/api/health/ready` reports PostgreSQL, object storage and Temporal ready.
- Reapproving exact Lithium dossier version 5 reconciled it as approved version 6, batch-approved its 14 supported claims, and automatically started `script-generation-approval-f67ac3248ce54edcbec8dbd68aefd30f-v6` through the normal script-generation gate.
- A one-time deployment reconciliation of the already-approved Lithium storyboard started `media-production-approval-reconcile-c2c5931f-v8` through the normal production workflow; no database row was fabricated outside workflow completion.
- A live browser check confirmed that approved research input no longer requires a second script/storyboard start action, private upload presents one `Approve & queue private upload` action, public release presents one `Approve release & schedule` action, and the separate downstream human gates remain visible.

2026-07-20 foundation evidence:

- `docker compose config --quiet` resolved eight core services.
- `docker compose ps` showed all eight services healthy; only Caddy publishes a host port (`8090`).
- `curl http://localhost:8090/api/health/ready` returned ready for PostgreSQL, object storage, and Temporal; `/` returned HTTP 200.
- API/core tests: 7 passed. Workflow/core tests: 6 passed. Next.js 16.2.10 production build and TypeScript validation passed.
- Live bootstrap established an Argon2id-backed administrator session. A viewer received `403` for user administration and a mutation without CSRF received `403`.
- Live YAML configuration import/export produced an immutable active version. Direct PostgreSQL `UPDATE audit_events` and `DELETE configuration_versions` both failed with `<table> is append-only`.
- Workflow `durability-probe-increment-0-gate-20260720` remained queryable with the same start time after `workflow-worker` restart, completed, and reconciled a repeat completion without a duplicate side effect.
- `scripts/smoke-core.sh` and `scripts/verify-durable-workflow.sh` reproduced the HTTP readiness and durable-restart gates; the latter tolerates the short interval between container HTTP health and resumed Temporal polling.
- Browser visual/accessibility evidence is missing because host npm/npx is not installed; the production web build and HTTP response are not being treated as visual evidence.

2026-07-20 Increment 1 evidence:

- Migrations through `0005_workflow_controls` are live with 22 application tables, pgvector, and database immutability triggers for source/evidence/chunk/transition/provenance/workflow-control records.
- Core, API, workflow and research suites contain 67 unique passing Python tests (19 core, 13 API, 6 workflow-worker and 29 research-worker). The Next.js production build and TypeScript gate pass with profile, Opportunity Board, source browser, dossier/claim ledger and workflow monitor screens.
- Core plus the nine-service research profile is healthy with private SearXNG, a separate Temporal research worker and self-hosted Firecrawl; all 17 services are healthy and only Caddy publishes a host port.
- Live `live-discovery` Temporal smoke executed four search strategies through private SearXNG, normalized and scored pending opportunities, and committed 20 append-only opportunity/source provenance rows in the observed run. The public result count is nondeterministic and this smoke is not a CI fixture.
- The public-source connector pins validated public DNS answers into the aiohttp connector and applies scheme/credential/port/domain, robots, redirect, TLS, MIME, byte, timeout, cookie and throttle controls. A live approval/acquisition drill stored a 79,383-byte GOV.UK raw object plus a 5,806-byte normalized object, persisted the SHA-256 snapshot with robots/DNS metadata, rejected a direct snapshot update, and reused the Temporal workflow on retry.
- Opportunity approval now records `DISCOVERY -> SHORTLISTED`; acquisition completion records `SHORTLISTED -> RESEARCHING`. Reject/defer decisions map to explicit interruption stages and every decision is version-checked and audited.
- `scripts/verify-discovery-fixture.sh` created a subject, generated a validated multi-strategy/falsification SearchPlan, deduplicated one of four findings, scored an opportunity with visible components/penalties, stored three raw and normalized MinIO snapshots, and produced a reviewable cited dossier.
- The dossier contained two independent direct supports (including primary evidence) and one contradiction, exact excerpts, source URLs and SHA-256 snapshot hashes. Retry preserved one dossier/idempotency result. Direct `UPDATE source_snapshots` failed with `source_snapshots is append-only`.
- Reviewer/admin claim and dossier approval succeeded with optimistic versions and audit records; a viewer review attempt returned `403`.
- Temporal subject schedule reconciliation is runtime-verified: two manual schedule triggers produced distinct run-scoped idempotency records, and disabling the subject paused the same schedule with both reconciliations audited.
- A live GOV.UK immutable snapshot produced dossier `bc55a4fe-de24-4790-b70e-b3c5a693630c` with two exact-offset claims. The configured high-risk rule correctly blocked completion for one independent/non-primary source; approval returned `409` without an override and succeeded only with a reasoned audited override. Retrying the start returned the same workflow and left exactly one dossier, idempotency row and `RESEARCHING -> DOSSIER_REVIEW` transition.
- The same snapshot was indexed through the authorized `source-semantic-index` workflow into nine immutable, stable-offset pgvector chunks using local `feature-hash-v1` embeddings. Retry preserved one idempotency record, vector self-distance was zero, the source-browser API exposed snapshot/chunk metadata, and normalized preview text was explicitly marked untrusted.
- Trafilatura 2.1.0 HTML, pypdf 6.14.2 PDF, canonical JSON, defused XML, bounded CSV and isolated text extractors are implemented. Tests cover injection retention, active-element removal, encrypted/malformed PDF rejection, XML entities, structured nesting and CSV normalization limits. A live 15,877-byte GOV.UK JSON source produced canonical normalized JSON and 22 immutable semantic chunks through Temporal/MinIO/PostgreSQL.
- Firecrawl is pinned by image digest at upstream build SHA `93387da4fbdf9de199649086efc637257e630397`. Its internal Playwright browser cannot open a direct public socket (`ENETUNREACH`), its DNS-pinning proxy returned `403` for loopback and metadata targets, and public CONNECT returned `200`. A live JS-only source produced 89 direct characters, 1,000 rendered/normalized characters, verified separate raw/rendered/composite hashes, two semantic chunks, and exactly one workflow/snapshot/run/idempotency record on replay.
- Redirect traversal now preserves an origin's explicit directory slash and rejects HTTPS-to-HTTP downgrades; both paths have regression tests and the downgrade was live-observed against the browser fixture.
- Multi-source dossier `3715eeb2-1d76-4068-bea7-22e730a3cec2` persisted an automatic `exact_duplicate` lineage edge for identical acquired evidence. All eight evidence links were marked non-independent, the completion rule reported zero independent supporting sources, and replay preserved one dossier, one relationship and one idempotency result. Deterministic tests separately prove the local cosine threshold produces `near_duplicate` lineage and prevents both related documents from counting independently.
- Robots policies are cached by origin and agent in a bounded one-hour LRU-style cache; concurrent checks coalesce behind a per-origin lock and each target path is still evaluated against the cached rules. Public acquisition rejects non-identity encodings before parsing. Controlled aiohttp fixtures verify allowed redirects, redirect exhaustion, MIME rejection, declared/streamed byte ceilings and encoding rejection; a mixed public/private DNS response rejects the entire answer set.
- Migration `0005_workflow_controls` adds immutable, non-secret workflow input provenance and parent lineage. Authenticated SSE through Caddy streamed workflow `live-discovery-control-gate-20260720202818`; operator cancellation reached `CANCELLED`, retry started child `live-discovery-retry-control-gate-20260720202818-retry`, repeated retry reconciled that child, and the child completed `OPPORTUNITY_REVIEW`. Exactly two control rows preserve the parent edge. Safe log history contains only whitelisted lifecycle/activity messages, and SSE releases its database session before remaining open.

2026-07-20 Increment 2 evidence:

- Migrations through `0010_scene_alternatives` are live with 39 application tables. Prompt, routing, usage, script, segment, claim-link, storyboard, scene-version, approval and generated-alternative records are append-only where required. A disposable empty database migrated from 0001 through 0010 and contained the `scene_alternatives_immutable` trigger.
- The provider-neutral gateway implements fake/CI, OpenAI-compatible, Ollama-native, Anthropic-style, Gemini-style and generic REST/JSON drivers. It validates configured/visible routes and one active prompt, enforces local/remote data policy before serialization, performs deterministic remote redaction, validates response schemas, bounds repair/retry/deadline behavior, uses fallback/circuit/concurrency/sliding-minute-rate controls, resolves only mounted secret basenames and persists safe usage/correlation/redaction records.
- The provider UI configures endpoints, mounted secret references, visible models, task routing and immutable prompts. It exposes prompt version comparison and the safe usage ledger, including redaction category/path/value hash without revealing removed values or credentials.
- Six isolated suites pass with 88 unique Python tests: 24 core, 7 AI gateway, 18 API, 1 foundation workflow-worker, 29 research-worker and 9 editorial-worker. The Next.js production image passed TypeScript validation and optimized build.
- Live supported workflow `script-generation-inc2-supported-20260720214553` produced eight strict segments, eight claim links, 100% central-claim coverage, no issues and exact-hash approval. Unsupported workflow `script-generation-inc2-unsupported-20260720215538` persisted a blocked draft with `unsupported_factual_statement` and `missing_evidence_excerpt`; approval returned `409`.
- A manual CTA edit created immutable script v2, independent verification created v3 with the same content hash and 100% coverage, and approval bound that exact v3 hash while v1/v2 remained intact. Selected regeneration workflow `script-regeneration-inc2-selection-202607202238` changed only `call_to_action`; the other seven segment hashes exactly matched the parent. Re-verification created v5 and exact-hash approval succeeded.
- The strict storyboard gate produced eight ordered, claim/source-linked SceneSpecs. A locked scene edit returned `409`; a one-scene edit cloned all eight scene records into storyboard v2 and invalidated aggregate approval. Generated-alternative workflow `scene-alternative-generation-inc2-alt-202607202249` stored an immutable candidate tied to the exact base scene. Selecting it cloned all eight scenes into storyboard v3: only `scene-001` changed, hashes for scenes 002–008 remained identical, the old candidate became historical and exact-hash approval succeeded.
- Viewer access to script/storyboard reads returned `200`; provider configuration and scene mutation returned `403`. AI model calls, version changes, approvals, workflow starts and alternative selection have correlation-linked audit records.

## Recovery and idempotence

2026-07-21 Increment 3 evidence:

- Migrations through `0013_narration_regen` are live. Core/API/editorial-worker/renderer tests and the Next.js optimized build pass.
- Approved storyboard hash `10aeed141148667cb201c6bf4cd7af51ed975abcbc775a6a5af9eb8a15728633` produced deterministic preview and 1080p H.264/AAC renders, aligned captions, source/chapter/thumbnail artifacts and immutable manifests.
- Enhanced FFmpeg QA checked stream/probe, duration, silence, black frames, freezes, caption reading rate, timing and visual overlap. Exact render/manifest approval `99d89998-4259-4b74-95c8-101991f4b4d8` was recorded only after mandatory checks passed.
- Scene and narration selection workflows produced new immutable assets while the parent approved production and every unselected scene/segment remained unchanged.

2026-07-21 Increment 4 evidence:

- Migration `0014_youtube_publishing` is live; 65 core/API tests and three publisher tests pass. The publishing profile, publisher worker and API are healthy, and the web UI passes TypeScript plus optimized production build.
- The dedicated gateway keeps the OAuth client secret and plaintext tokens behind publishing egress. Refresh tokens and resumable URIs are encrypted with the separately mounted key; OAuth state is single-use and time bounded.
- Live dry-run publication `b7c21cc2-b184-4a1b-9f25-130fbbf10739` received video ID `dry_271b9c7d04c`. Two starts with different client retry keys resolved to one publication, one video ID and one stable provider key. Caption/thumbnail attachment completed and reconciliation reached `processed` while privacy remained `private`.
- Changing metadata produced hash `f73415669b3bc23bd891b58160d06d1d6c5f446c1514366c580e3386a3eb2ba6`; upload returned `409` until that exact version receives a new approval. A real request returned `403` under the dry-run configuration. No Google video was created.

Compose stop/start must preserve named volumes. Migrations are forward-only and repeatable. External side effects use stable idempotency keys and reconciliation records. Never use `docker compose down -v` in normal operations. Backup/restore tooling has been exercised against the complete demo project and refuses to overwrite production targets.

2026-07-21 Increment 5 and final-audit evidence:

- Immutable analytics, model benchmark/recommendation review/application, source freshness, originality, corrections, budgets and operational-evidence APIs/UI are complete. Recommendation never mutates routing automatically.
- Assisted/supervised/trusted policy, sensitive-topic mandatory gates and required opportunity rationale are deterministic, UI-configurable and approval-bound.
- Local/TOTP/OIDC authentication, issuer-bound OIDC subjects, durable login throttling, Caddy body ceiling and nonce CSP are implemented.
- OTel collector, Prometheus, Grafana, Loki and Promtail are healthy; all Prometheus targets were up and trace/log ingestion was observed.
- A clean empty database migrated through `0018_operating_policy` to 75 tables with the immutable-trigger set.
- Full final suites passed: 44 core, 84 API, 7 AI gateway, 6 workflow, 29 research, 13 editorial/media, 3 publisher and 2 renderer tests; the web TypeScript and production build pass.
- Security audit proves one host port, internal private networks, read-only/capability-dropped application services and dedicated identity/provider/publishing egress membership.
- The final 26-image checksummed CycloneDX SBOM is `/srv/youtuber-backups/sbom-final-20260721T0818`; its 26-entry manifest SHA-256 is `0a0de726474b163584cfb9077c544683fc83b4c09690c4d194c8d6361589b5be`.
- The post-`0018` backup/restore artifact is `/srv/youtuber-backups/final-20260721T0822`; the disposable drill restored 75 tables, 148 audit events, eight source snapshots and 185 objects, removed its temporary targets and did not modify production. Real YouTube OAuth/upload was not invoked without operator-owned credentials.

2026-07-21 channel and subject lifecycle evidence:

- Every active channel and subject card exposes an accessible Edit action with a prefilled complete profile editor. Saves use the existing optimistic version check, preserve linked history and immediately refresh both the card list and channel workspace selector.
- Archive is an audited, version-checked soft deletion. Channel archival is blocked while active subjects remain; subject archival first reconciles its Temporal schedule to disabled/paused and records the resulting schedule state in the audit context.
- API regression passed with 93 tests. The Next.js TypeScript check and optimized production build passed, and the rebuilt API and web images are deployed.
- A disposable live channel was edited from version 1 to 2. Its disposable subject was edited from version 1 to 2, enabled, reconciled to an active `0 4 * * *` schedule, then archived; PostgreSQL confirmed `schedule_paused=true`, `enabled=false`, `deleted_at` set and version 4. The channel was then archived at version 3, disappeared from the workspace selector, and the selected scope safely reset to All channels.
- Desktop and 390-pixel browser checks confirmed the prefilled editors and dependency-aware archive dialog. The browser console reported zero errors and zero warnings; all 25 deployed services were running without unhealthy or starting containers.

2026-07-21 archived profiles and scoring-trace evidence:

- The Administration navigation now exposes a searchable Archived profiles workspace to readers. It filters by profile type and identifying content, exposes complete archived configuration on demand, and limits restore controls to editors and administrators.
- Restore is audited and optimistic-version checked. A restored channel remains disabled; a subject restore is blocked until its parent channel is active, and a restored subject remains disabled so its existing cron cannot run without an explicit later enable action.
- A disposable archived channel and subject were restored through the live UI in dependency order, verified as disabled, and archived again. PostgreSQL retained both restore and archive events; the subject was again archived before its parent channel.
- Live-discovery scoring now derives all seven positive components and three penalties from observed rank, topic/query coverage, source count/type/diversity, content specificity, publication freshness, purpose, domain rarity and prior-finding similarity. Stored reasons include the observed inputs rather than generic constant text.
- The append-only upgrade added score version 2 to all 191 legacy placeholder-scored opportunities and preserved every original score row. For 19 records without source links, the trace explicitly uses stored title/summary/subject signals with zero-source, unavailable-rank and unavailable-date inputs rather than inventing provenance.
- Before the upgrade, 175 active opportunities had the identical 53 trace. Current latest live-discovery scores span 30 integer values from 26 through 74; 19 round to 53 naturally, with varied component and reason traces, and no latest score retains the placeholder component set.
- API regression passed with 96 tests, research-worker regression passed with 40 tests, and the Next.js TypeScript and optimized production build gates passed. Rebuilt API, research-worker and web services are healthy in the 25-service deployment.
- Browser checks at desktop and 390 pixels confirmed archived search/empty states, dependency-aware restore controls and score-version-2 detail presentation. A live 51/100 finding showed seven observed components, three penalties and finding-specific reasons; the TubeFactory browser console reported zero errors and zero warnings.

2026-07-21 FaktischSimpel workflow evidence:

- Channel version 3 contains validated workflow `faktischsimpel.explainer` with 13 visible production stages, separate Writer, independent Verifier and Storyboard prompts, and all six mandatory human gates. Compiled instruction lengths are 2,389, 1,910 and 1,773 characters, below the 5,000-character task boundary.
- The worker resolves the channel through dossier/opportunity/subject lineage for initial generation, regeneration, verification, storyboard generation and scene alternatives. Channel instructions remain bounded style refinements layered beneath global evidence, schema, validation and private-publication contracts.
- `FaktischSimpel Themenradar` was created disabled, its six-strategy search plan and falsification branch were reviewed, then version 3 was enabled. PostgreSQL confirmed Cron `0 7 * * 1`, timezone `Europe/Berlin` and an active Temporal schedule.
- The original empty run was traced to SearXNG returning HTTP 200 with every selected engine unavailable; the former client discarded that health signal. Discovery now retains per-engine health, spaces seven searches sequentially, retries bounded transient failures, applies the recent time filter only to broad discovery, and fails visibly with `SOURCE_UNAVAILABLE` when no selected engine responds.
- Filtered live gate `live-discovery-live-gate-20260721T175827-3173031` completed healthy at `OPPORTUNITY_REVIEW`: 70 raw results, 18 explainer-eligible, 52 rejected as irrelevant, 16 deduplicated, 11 new opportunities, five already processed, and three responding engines (`arxiv`, `bing`, `bing news`). A subsequent browser-triggered run also completed healthy with two new and 15 previously processed candidates.
- Core plus API regression passed with 102 tests; research-worker regression passed with 45 tests; editorial-worker regression passed with 16 tests; publisher-worker regression passed with 3 tests. The web TypeScript test and production Next.js build passed, and API, web and research-worker are healthy after deployment.
- Playwright captured the live 13-stage workflow, full Writer prompt, corrected localized search plan, active schedule, searchable six-step task guide and completed discovery source-health trace. The corrected illustrated 11-page A4 DOCX is `docs/FaktischSimpel-Automatisierungsworkflow.docx`; LibreOffice conversion and visual inspection confirmed readable screenshots, validation tables and exact prompt appendix.

2026-07-21 hybrid LAN-AI optimization evidence:

- The stale `script-generation-retry-post-ai-reboot-20260721-2017` execution was cancelled with its Temporal and application audit history intact.
- Migration `0019_hybrid_ai_reviews` is live. Topic qualifications and dossier evidence assessments are append-only and retain model, prompt, rubric, confidence, abstention and resulting score-version provenance.
- Worker-side provider discovery returned 30 models from the OpenAI-compatible `:11438/v1/models` endpoint and exposed `gemma4-e4b:64k`; a temporarily offline server returns an actionable HTTP 503 without changing stored configuration.
- Active model assignments for `script_writer`, `script_verifier`, `storyboard`, `topic_qualifier` and `evidence_reviewer` all resolve to local model `gemma4-e4b:64k`, with no remote fallback.
- The prior four active and 38 legacy FaktischSimpel findings are recoverably archived: 42 archived and zero active before the new series. Linked sources, dossiers, scores, usage and audit history remain retained.
- Discovery `live-discovery-faktischsimpel-hybrid-lan-dedupe-fix-20260721T211627` completed healthy with 70 raw results, 17 active opportunities, 17 LAN-AI qualifications, 13 deterministic hybrid score versions and four explicit abstentions.
- The highest non-abstained result, geothermal lithium extraction, advanced through human opportunity review and automatic acquisition. Six immutable evidence snapshots fed dossier v2 and local AI assessment `445c45f5-3b34-518d-8886-9dc0a33062bd`; the model abstained instead of laundering one underlying paper into independent confirmation.
- Validation passed with 81 core/gateway/editorial tests, 51 research-worker tests, 54 API tests, Next.js TypeScript and optimized production build. API, web, editorial worker and research worker are healthy after deployment.

2026-07-22 bounded script correction and storyboard completion evidence:

- All seven active editorial and research task assignments resolve `gemma4-e4b:64k` through the OpenAI-compatible provider at `http://ai.b1.germering:11438/v1`; each assignment retains the same model through the native Ollama provider at `http://ai.b1.germering:11434` as its protocol-correct local fallback.
- Dossier `b9d57783-422f-4de4-b7e7-efb5ed54d7fd` version 2 is approved with three evidence-linked claims and 100% coverage.
- The bounded self-correction run produced approved script v31, version `ba9a2242-99c0-41af-a8f1-05141b1511cf`, exact hash `7f5e454c6330968b0cf6882306c88785dd56b9ce752d955e49dd1d17ee66846b`: eight segments, 410 words, 182.2 seconds, 100% central-claim coverage, zero errors and zero warnings.
- The source-linked storyboard produced approved v8, version `c2c5931f-00fc-49a3-9ef1-e8b1596d2fd4`, exact hash `1a54a75c75303e2d058ec2a29ec65800d4551f9d1549ee158c59bab3ba14bb84`: eight scenes totaling 182.2 seconds and seven distinct visual roles with explicit claim/source bindings.
- Two AI verification passes are combined with deterministic source-gap, repeated-fact and exact-evidence gates. Contradictory model verdict fields are normalized from the finding severities; editorial statements remain claim-free, and unresolved factual failures remain blocked.
- Storyboard generation now assigns a deterministic visual-diversity plan when model output is monotonous and validates compact alternatives against exact trusted visual-type instructions before assembling immutable SceneSpecs.
- The stale `youtuber-editorial-worker-1` was stopped after it was found consuming the same Temporal queue. Only `tubefactory-editorial-worker-1` remains active and healthy.
- Combined AI gateway and editorial-worker regression passed with 56 tests. Playwright captured the exact approved hashes, the complete v31 script and the complete v8 storyboard for the updated illustrated DOCX runbook.

2026-07-22 Explanation Readiness and autonomous-series evidence:

- Migration `0021_explanation_readiness` is live. Dossiers retain a versioned explanation plan and readiness evaluation, claims retain covered unit IDs, and AI query plans retain the units that each query targets.
- The active FaktischSimpel subject policy is 480 seconds target, 360–600 seconds allowed, 135 words/minute, three enrichment rounds, six explanation units and 1.5 factual claims per 100 words. This deterministically requires 810–1,350 words and at least 13 factual claims.
- Core (58), API (112), research-worker (61) and editorial-worker (43) test suites passed. The web TypeScript and optimized Next.js build passed; `git diff --check`, Python compilation and the rebuilt illustrated DOCX integrity checks passed.
- API, web, research worker and editorial worker are healthy after deployment. All seven AI tasks route `gemma4-e4b:64k` to `http://ai.b1.germering:11438/v1` with native Ollama fallback at `http://ai.b1.germering:11434`.
- Live workflow `live-research-dossier-readiness-v4-final-auto-20260722a` completed after four LAN-AI syntheses for Lithium, persisted 3/13 usable claims from a deterministic 24-source evidence set, and automatically selected approved opportunity `daf9286a-2c60-48fc-a6e7-06b430477d50` without database manipulation.
- The append-only AI usage ledger records 17 successful calls for the parent/child series across `research_query_planner`, `evidence_synthesizer` and `evidence_reviewer`; every record names provider `B1 Local AI` and model `Gemma 4 E4B 64K`.
- The child workflow automatically created acquisition and research lineage for the sitting/cancer candidate, acquired 22 immutable snapshots, completed four LAN-AI syntheses and ended with 6/13 usable claims, eight independent sources, five primary sources, all six explanation units and counterevidence search complete. With no further approved candidate, the parent completed in `INSUFFICIENT_EXPLANATION_EVIDENCE`; no script was generated.
- Playwright captured the policy editor, legacy-dossier gate, Lithium readiness gaps and autonomously selected candidate result. The illustrated DOCX contains 13 figures and documents the retained v31/v8 artifacts explicitly as a pre-readiness baseline rather than a currently acceptable result.
